meta {
  name: Listar todos os clientes
  type: http
  seq: 1
}

get {
  url: http://localhost:8081/clientes
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
